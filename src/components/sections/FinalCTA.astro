<section class="final-cta" aria-labelledby="final-cta-title">
  <div class="final-cta-inner">
    <p class="eyebrow">TU PRÓXIMO VIAJE</p>

    <h2 id="final-cta-title">
      ¿Tienes un viaje<br />
      en mente?
    </h2>

    <p class="description">
      Cuéntanos destino, fechas aproximadas y cómo te gustaría viajar.
      No necesitas tenerlo todo decidido: parte de nuestro trabajo es ayudarte a ordenar las decisiones.
    </p>

    <a href="/cuentatuviaje" class="cta-button" data-track="cta_travel_request" data-track-label="Home — cuéntanos tu viaje">
      Cuéntanos tu viaje
    </a>

    <p class="reassurance">
      Sin itinerario previo. Sin tener que saber exactamente qué pedir. Empezamos entendiendo el viaje que tienes en mente.
    </p>
  </div>
</section>

<style>
  .final-cta {
    padding: 10rem 2rem;
    background: var(--color-papel);
    text-align: center;
  }

  .final-cta-inner {
    max-width: 820px;
    margin: 0 auto;
  }

  .eyebrow {
    margin: 0 0 1.5rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    font-size: .8rem;
    color: var(--color-cobre);
  }

  h2 {
    margin: 0;
    font-size: 4.5rem;
    line-height: 1.05;
  }

  .description {
    max-width: 650px;
    margin: 2rem auto 0;
    font-size: 1.15rem;
    line-height: 1.8;
    color: #555;
  }

  .cta-button {
    display: inline-block;
    margin-top: 3rem;
    padding: 1rem 2rem;
    border-radius: 999px;
    background: var(--color-bosque);
    color: white;
    text-decoration: none;
    font-weight: 600;
    transition: transform .2s ease, opacity .2s ease;
  }

  .cta-button:hover,
  .cta-button:focus-visible {
    transform: translateY(-2px);
    opacity: .92;
  }

  .reassurance {
    max-width: 620px;
    margin: 1.5rem auto 0;
    color: #666;
    font-size: .9rem;
    line-height: 1.65;
  }

  @media (max-width: 800px) {
    .final-cta { padding: 7rem 1.5rem; }
    h2 { font-size: 3.2rem; }
  }

  @media (prefers-reduced-motion: reduce) {
    .cta-button { transition: none; }
  }
</style>
