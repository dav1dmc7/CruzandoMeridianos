<section class="work-preview" aria-labelledby="work-preview-title">
  <div class="work-preview-inner">
    <div class="work-preview-heading">
      <p class="eyebrow">ASÍ SE VE NUESTRO TRABAJO</p>
      <h2 id="work-preview-title">De muchas posibilidades a una decisión que tiene sentido.</h2>
      <p>
        No publicamos una ruta privada disfrazada de ejemplo. Enseñamos el tipo de trabajo que hacemos para que veas qué cambia cuando alguien cruza las variables por ti.
      </p>
    </div>

    <div class="work-flow" aria-label="Ejemplo del proceso de diseño de un viaje">
      <article class="work-step">
        <span class="step-number">01</span>
        <p class="step-label">ANTES</p>
        <h3>Demasiadas opciones</h3>
        <p>Destinos, alojamientos, transportes y planes compiten entre sí. Todo parece posible y nada termina de encajar del todo.</p>
      </article>

      <div class="work-arrow" aria-hidden="true">→</div>

      <article class="work-step work-step-featured">
        <span class="step-number">02</span>
        <p class="step-label">NUESTRO TRABAJO</p>
        <h3>Investigamos y cruzamos</h3>
        <p>Filtramos según vuestro tiempo, ritmo, prioridades y límites. Contrastamos lo que importa y descartamos lo que sobra.</p>
      </article>

      <div class="work-arrow" aria-hidden="true">→</div>

      <article class="work-step">
        <span class="step-number">03</span>
        <p class="step-label">DESPUÉS</p>
        <h3>Decisiones más claras</h3>
        <p>Recibes una propuesta con sentido, alternativas cuando aportan valor y el porqué de las decisiones importantes.</p>
      </article>
    </div>

    <div class="work-preview-footer">
      <span>No necesitas copiar nuestra investigación. Necesitas beneficiarte de ella.</span>
      <a href="/como-trabajamos" data-track="interaction" data-track-label="WorkPreview · Ver cómo trabajamos">Ver cómo trabajamos <span aria-hidden="true">↗</span></a>
    </div>
  </div>
</section>

<style>
  .work-preview {
    padding: clamp(6rem, 9vw, 9rem) 2rem;
    background: var(--color-bosque);
    color: var(--color-papel);
  }

  .work-preview-inner {
    width: min(var(--content-width), 100%);
    margin: 0 auto;
  }

  .work-preview-heading {
    display: grid;
    grid-template-columns: minmax(0, .95fr) minmax(280px, 1.05fr);
    gap: clamp(3rem, 8vw, 8rem);
    align-items: end;
  }

  .eyebrow {
    grid-column: 1 / -1;
    margin: 0;
    color: var(--color-cobre);
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .work-preview h2 {
    margin: 1.2rem 0 0;
    color: var(--color-papel);
    font-family: var(--font-serif);
    font-size: clamp(3rem, 5vw, 5.2rem);
    font-weight: 400;
    line-height: .96;
    letter-spacing: -.04em;
  }

  .work-preview-heading > p:last-child {
    margin: 0;
    color: rgba(251,249,245,.68);
    font-size: 1rem;
    line-height: 1.85;
  }

  .work-flow {
    display: grid;
    grid-template-columns: minmax(0,1fr) auto minmax(0,1.12fr) auto minmax(0,1fr);
    gap: 1.5rem;
    align-items: stretch;
    margin-top: clamp(4rem, 7vw, 6rem);
  }

  .work-step {
    min-height: 260px;
    padding: 1.5rem 0;
    border-top: 1px solid rgba(251,249,245,.18);
  }

  .work-step-featured {
    padding: 1.5rem 1.5rem 1.75rem;
    border: 1px solid rgba(194,125,56,.5);
    background: rgba(251,249,245,.05);
  }

  .step-number {
    display: block;
    color: var(--color-cobre);
    font-family: var(--font-serif);
    font-size: 2.5rem;
    line-height: 1;
  }

  .step-label {
    margin: 1.4rem 0 .75rem;
    color: rgba(251,249,245,.5);
    font-size: .68rem;
    font-weight: 600;
    letter-spacing: 1.8px;
  }

  .work-step h3 {
    margin: 0 0 .75rem;
    color: var(--color-papel);
    font-family: var(--font-serif);
    font-size: 2rem;
    font-weight: 400;
    line-height: 1;
  }

  .work-step p:last-child {
    margin: 0;
    color: rgba(251,249,245,.62);
    font-size: .92rem;
    line-height: 1.75;
  }

  .work-arrow {
    align-self: center;
    color: rgba(251,249,245,.35);
    font-size: 1.2rem;
  }

  .work-preview-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(251,249,245,.12);
  }

  .work-preview-footer span {
    color: rgba(251,249,245,.52);
    font-size: .82rem;
    line-height: 1.6;
  }

  .work-preview-footer a {
    display: inline-flex;
    align-items: center;
    gap: .6rem;
    flex: 0 0 auto;
    color: var(--color-papel);
    text-decoration: none;
    font-size: .86rem;
    font-weight: 600;
  }

  .work-preview-footer a span {
    color: var(--color-cobre);
    transition: transform .2s ease;
  }

  .work-preview-footer a:hover span {
    transform: translate(3px,-3px);
  }

  .work-preview-footer a:focus-visible {
    outline: 2px solid var(--color-cobre);
    outline-offset: 4px;
  }

  @media (max-width: 900px) {
    .work-preview-heading {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .work-preview h2 {
      margin-top: 0;
    }

    .work-flow {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .work-arrow {
      display: none;
    }

    .work-step,
    .work-step-featured {
      min-height: 0;
      padding: 1.5rem 0;
      border-left: 0;
      border-right: 0;
    }

    .work-step-featured {
      padding: 1.5rem;
    }
  }

  @media (max-width: 620px) {
    .work-preview {
      padding: 5.5rem 1.25rem;
    }

    .work-preview-footer {
      align-items: flex-start;
      flex-direction: column;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .work-preview-footer a span {
      transition: none;
    }
  }
</style>
